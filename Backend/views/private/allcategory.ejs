<div class="text-left mt-2">
    <a href="/category/addcategory">
        <button class="btn btn-dark" type="button">افزودن</button>
    </a>
</div>

<% if (allcategory.length> 0) { %>
    <h2 class="mt-2"> لیست دسته بندی ها</h2>
    <div class="table-responsive">
        <table class="table table-striped table-sm">
            <thead>
                <tr>
                    <th>#</th>
                    <th>عنوان</th>
                    <th>توضیحات</th>
                    <th>والد</th>
                    <th>تصویر</th>
                    <th></th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                <% for (let list of allcategory) { %>
                    <tr>
                        <td>
                            <%= list._id %>
                        </td>
                        <td><a href="/usersgroup/edit-usersgroup/<%= list._id %>">
                                <%= list.title %>
                            </a></td>

                        <td>
                            <%= list.parent %>
                        </td>
                        <td>
                            <%= list.desc %>
                        </td>

                        <td>
                            <%= list.thumbnail %>
                        </td>
                        <td>
                            <a href="/usersgroup/edit-usersgroup/<%= list._id %>"
                                class="btn btn-sm btn-primary">ویرایش</a>
                        </td>
                        <td>
                            <a href="/usersgroup/delete-usersgroup/<%= list._id %>"
                                class="btn btn-sm btn-danger">حذف</a>
                        </td>
                    </tr>
                    <% } %>
            </tbody>
        </table>
        <%- include("../includes/pagination.ejs") %>
    </div>
    <% } else { %>
        <div class="text-center">
            <h3 class="mt-2">
                در حال حاضر هیچ دسته بندی در پایگاه داده ثبت نشده است
            </h3>
        </div>
        <% } %>